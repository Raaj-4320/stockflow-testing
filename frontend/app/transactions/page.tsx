import { PageHeader } from '@/shared/components/PageHeader';

export default function TransactionsPage() {
  return (
    <section>
      <PageHeader title="Transactions" subtitle="Placeholder route for future migration." />
      <div className="sf-card">No transaction logic integration in this phase.</div>
    </section>
  );
}
