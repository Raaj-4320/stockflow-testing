import { PageHeader } from '@/shared/components/PageHeader';

export default function InventoryPage() {
  return (
    <section>
      <PageHeader title="Inventory" subtitle="Placeholder route for future migration." />
      <div className="sf-card">No inventory backend wiring in this phase.</div>
    </section>
  );
}
